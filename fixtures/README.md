# Synthetic fixtures

Every file in this directory is invented solely for local development and demonstrations. They do not contain, derive from, or reference personal application materials.

Fixtures must remain JSON documents with `"fixture": true` and `"source": "synthetic"`; `npm run validate:config` enforces these markers.
